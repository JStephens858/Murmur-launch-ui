// Temporary alias: /videos serves the grid prototype (/videos2) until the
// videos1-vs-videos2 verdict is in — see DECISIONS.md. Navigation links
// point here so they won't need to change.
export { default } from "../videos2/page";
